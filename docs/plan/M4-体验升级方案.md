# M4 · 体验升级方案（回答溯源 + 持久化记忆 + 前端升级）

> 前置：M2 完成（sources 已随 done 事件透传）、M3 完成（agent 模式可切换）
> 目标：回答可溯源（引用面板可见）、会话记忆持久化（重启不丢）、前端具备 Agent 模式开关
> 验收：提问后页面下方展示引用来源列表；重启服务后会话上下文仍在；Agent 开关可切换三种问答模式

## 一、范围

**做**
- 溯源 UI：done 事件携带 sources（M2 已透传），前端渲染来源面板；正文 [n] 角标样式化
- 持久化记忆：Redis 存储（会话消息 JSON，TTL 7 天），配置可切换 `memory | redis`，接口不变
- 前端升级：问答模式切换（普通 / RAG / Agent）、新会话按钮（重置 sessionId）、来源面板
- 重启后 SSE 断线不白屏（前端自动提示重连）

**不做**
- 不做多会话历史列表 UI（会话数据都在 Redis，接口预留 GET /api/conversations）
- 不做记忆摘要压缩 / 长期向量记忆（已在 MVP 方案明确砍掉）
- 不做用户体系与鉴权

## 二、核心设计

### 溯源数据流（打通 M2 遗留）

```
M2：HybridRetriever → RetrievedChunk(chunkId, content, docName, rrfScore)
     → ChatController 组装 sources: [{n, docName, snippet(前80字)}]
     → done 事件 data: {sessionId, sources}
M4：前端 done 处理 → 渲染来源面板（编号/文档名/片段预览）
     → 正文 [n] 用 <sup> 角标样式
```

- Agent 模式（M3）若经过 search_knowledge_base 工具，同样收集其 sources 随 done 下发（AgentContext 增加 sources 收集器）

### 持久化记忆（Redis）

```java
public interface ConversationMemory {                 // 从 @Component 抽成接口
    List<ChatMessage> load(String sessionId, int maxMessages);
    void append(String sessionId, ChatMessage message);
    void clear(String sessionId);
}
```

- `InMemoryConversationMemory`（现状实现，默认）
- `RedisConversationMemory`：`StringRedisTemplate`，key = `chat:memory:{sessionId}`，value = JSON 数组
  - append：读出 → 追加 → 写回（每条全量重写，消息量 ≤20 条，成本可忽略），EXPIRE 7 天
  - 简单可靠，避免引入 Stream/List 结构的复杂度
- 配置：`rag.memory.type: memory | redis`，`@Configuration` 按类型装配（单 Bean 条件装配）

### 前端升级（index.html）

- 顶部模式切换：三选一（普通对话 / 知识库问答 / Agent），对应 `chat` 请求参数 `mode: plain | rag | agent`
- 请求体统一为 `{sessionId, message, mode}`（M2/M3 的 `kb`/`agent` 参数收敛为 `mode`，保留旧参数兼容一个版本）
- 新会话按钮：清空消息区 + 重新生成 sessionId + 清 Redis 记忆（DELETE /api/memory/{sessionId}）
- 来源面板：done 事件渲染，样式与气泡区一致

## 三、新增/修改文件清单

| 操作 | 文件 | 说明 |
|---|---|---|
| 修改 | pom.xml | + spring-boot-starter-data-redis |
| 新建 | memory/ConversationMemory.java | 从实现类抽出接口 |
| 修改 | memory/ConversationMemory.java → InMemoryConversationMemory.java | 现类改名实现接口 |
| 新建 | memory/RedisConversationMemory.java | Redis 实现 |
| 新建 | config/MemoryConfig.java | 按 rag.memory.type 装配 |
| 修改 | api/ChatController.java | mode 参数收敛 + DELETE /api/memory/{sessionId} + Agent sources 收集 |
| 修改 | application.yaml | + spring.data.redis、rag.memory.type |
| 修改 | static/index.html | 模式切换 / 新会话 / 来源面板 / [n] 角标 |
| 新建 | docker-compose.yml（追加 redis 服务） | M2 的 compose 加 redis:7-alpine |
| 新建 | test：RedisConversationMemoryTest（内嵌连本地或跳过）+ 记忆接口契约测试 | |

## 四、任务拆解

| 任务 | 步骤 | 验证 |
|---|---|---|
| T1 记忆接口化 | 抽接口 + 现类改名 + 配置装配 | `./mvnw test` 绿（行为不变回归） |
| T2 Redis 实现 | StringRedisTemplate JSON 读写 + TTL | 本地起 redis：append → 重启应用 → load 仍在 |
| T3 模式收敛 | mode 参数（plain/rag/agent）+ 兼容旧参数 | curl 三种 mode 行为各归其链路 |
| T4 溯源打通 | Agent sources 收集 + done 下发 | Agent 模式提问 → done 事件含 sources |
| T5 前端升级 | 模式切换/新会话/来源面板/角标 | 页面操作三种模式 + 来源面板渲染 |
| T6 记忆清理 | DELETE /api/memory/{sessionId} | 清除后 load 为空 |

## 五、验收标准

- [ ] RAG/Agent 模式提问后，页面底部展示引用来源（编号 + 文档名 + 片段）
- [ ] 正文 [n] 渲染为角标样式
- [ ] `rag.memory.type=redis` 时：重启应用，同一 sessionId 继续提问仍记得上文
- [ ] 新会话按钮：消息区清空、记忆清空、新 sessionId
- [ ] 三种模式切换后各自链路正确（普通不检索 / RAG 检索 / Agent 走工具循环）
- [ ] `rag.memory.type=memory` 时行为与 M3 完全一致（回归）
- [ ] 全量测试绿

## 六、关键取舍（面试可讲）

1. **记忆全量重写而非 Redis List 追加**：消息量小（N 轮窗口），全量 JSON 重写最简单且原子；不值得引入 Stream 复杂度
2. **接口抽象先行、实现可切换**：内存版保证零依赖可跑，Redis 版保证生产语义，配置一键切换
3. **sources 随 done 一次性下发而非增量**：来源数量少、时机在回答结束后，无需增量协议
